// eslint-disable-next-line no-unused-vars
export const reducer = (state, {type, action}) => {
    switch (type) {
        case 'plus':
            return state;
        default:
            return state;
    }
}